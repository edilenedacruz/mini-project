<h1>Create a new gif</h1>

<%= form_for(@gif, url: admin_gifs_path) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.submit "Add a gif", class: "button" %>
<% end %>

<%= button_to "Return to Admin user page", user_path(current_user), class: "button", method: :get %>
