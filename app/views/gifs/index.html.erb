<h1>List of gifs</h1>
<% @gifs.each do |gif| %>
<ul>
  <%= image_tag(gif.image_path) %><br>
  Category: <%= gif.title %><br>
  <%= link_to "Favorite this gif", {:controller => favorite_gifs_path, :action => "create", :gif_id => gif.id, :user_id => current_user.id}  %><br>

</ul>
<% end %>
